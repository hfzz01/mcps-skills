# Multica IOC 需求小队 · 智能体与流程设计

一套可直接装配到 **Multica 自托管实例** 上的「需求分析小队」：五个通用能力单元 + 一份配置驱动的流程控制文本，把"人写需求"变成"小队协作产出需求文档"。

**适用场景**：内网环境、只有自研 agent CLI（codeagent3.0，Claude Code 套壳，内置 GLM-5.2）、无任何外部模型 API key。

---

## ⚠️ 先读这三条平台红线

> 2026-09-14 首次内网实跑翻车（负责人变成成员、队长反复"首次运行"、运行中断）后查清并修正。
> 完整机制与排障见 [`09-平台机制与排障.md`](09-平台机制与排障.md)。

| 红线 | 违反的后果 |
|---|---|
| **负责人永远是小队。派活只能 @提及，绝不改负责人** | 小队失去持有权 ⇒ 成员的评论不再唤醒队长、队长失去改状态权限 ⇒ 链路静默断掉 |
| **成员交回队长 = 发一条不带任何 @ 的顶层评论** | 显式 @ 了别的目标（包括 @队长）会关掉平台的回落机制 ⇒ 队长永远不被唤醒 |
| **阶段与轮次写在 label 上；状态只用平台内置值** | 自造状态不继承平台行为；靠解析评论判进度会在噪声里飘 |

另外两条容易踩的：**人类要唤醒队长必须显式 @队长/@小队**（`/note` 和 `@all` 都不触发任何智能体）；**成员中途留痕要用 `/note` 开头**，否则普通评论会回落、白唤醒队长一轮。

---

## 设计要点

**1. 智能体不知道流程。** 五份提示词里找不到"第一轮""被打回后""交给审核"这类编排语言。顺序、循环、回退、挂载条件全部由小队的「流程控制」字段驱动——换个队、换套流程，提示词一个字都不用改。
（注意：这个字段系统**只注入给 leader**，成员读不到。所以成员自己的规则写在各自的提示词里。）

**2. 能力单元数 > 实际使用数。** 五个角色不是每单都跑五个。队长、写手、审核员常驻；**现状核对官与原型官是条件挂载**，不命中触发条件就整环跳过。所以角色多不等于流程长，多出来的只是"能力储备"。

**3. 事实清单的防错靠结构，不靠"写对它"。** 清单是模型读代码得出的结论，必然会有错条目。所以目标不是"永不犯错"，而是**错误进不了正文 + 进了正文也能精确定位**：前者靠置信度三档（`存疑`/`未找到` 不得进正文），后者靠文档附录的引用映射表（清单条目被推翻时，定点整改受影响章节，不必全文重审）。

**4. 两条独立计数线。** 机器审查 R1~R3 一条线（收敛阀门：第 3 轮只认阻塞级）；**人审返工**另一条线，不占三轮额度，人打回后写手按要求整改、交回直接再给人看，默认不重跑机器审查。

---

## 文件清单

| 文件 | 内容 | 装配到哪 |
|---|---|---|
| `00-总览与装配说明.md` | 总入口：平台红线、三层架构、装配表、完整链路图 | 阅读用 |
| `01-队长.md` | 队长提示词（通用流程执行器 + 平台机制红线） | 队长智能体的 Instructions |
| `02-写手.md` | 写手提示词（含事实清单用法、整改轮纪律、交接方式） | 写手智能体的 Instructions |
| `03-审核员.md` | 审核员提示词（审查 + 清单抽检 + 交接方式） | 审核员智能体的 Instructions |
| `04-小队流程控制.md` | **唯一真源**：平台约束、阶段、挂载条件、流转、状态与 label、派单模板、取值规则、异常分支 | **小队的「流程控制」字段**（整段填入；只有 leader 能读到） |
| `05-审查清单.md` | 需求文档审查清单（A~K 节，标【验伪】的必须进代码核实） | 做成技能「审查清单」→ 绑给审核员 |
| `06-现状核对官.md` | 现状核对官提示词（事实采集，只读不改） | 现状核对官智能体的 Instructions |
| `07-事实清单模板.md` | 事实清单输出格式（置信度三档、证据、覆盖声明、基线） | 做成技能「事实清单模板」→ 绑给现状核对官 |
| `08-原型官.md` | 原型官提示词（两条底座：截真系统 / OpenDesign MCP） | 原型官智能体的 Instructions |
| `09-平台机制与排障.md` | **给你看的**：Multica 小队/触发/状态机制、失败原因码对照、中断排查手册 | 不装配，排障时查 |

---

## 装配六步

1. 建五个智能体，把 `01` / `02` / `03` / `06` / `08` 分别填进各自的 Instructions
2. 把 `04` **整段**填进小队的「流程控制」字段
3. `05` 与 `07` 各做成一个技能，分别绑给审核员、现状核对官
4. 写手绑已有的 `req-doc` 技能；原型官绑原型技能（两条底座各一份，见 `08` 末尾）
5. 小队 leader 设为**队长**，成员为其余四个
6. 五个智能体挂到同一个 project（project 上指定本地代码仓路径）

⚠️ **装配后自检两条**：①issue 的负责人**永远是小队**，不是任何成员；②队长的提示词里**不许出现"指派给它"这类说法**，只有 @提及。

## 一条需求走完全程

```
issue 分配给小队（负责人 = 小队，全程不变）
  → 队长判断挂载条件，@提及 派活
  → [条件] 现状核对官产 facts.md →（不带 @ 交回）→ 队长形式门（只查字段齐不齐）
  → 写手出 reqdoc.md →（不带 @ 交回）→ 审核员审一轮
       ├─ 不通过 → 打回写手整改（轮次 +1，最多 3 轮；第 3 轮仅阻塞级）
       └─ 三轮全过 → [条件] 原型官产出原型
  → 状态→in_review（=等人审），@人
  → 人审 ┬─ @队长「通过」 → 状态→done
         └─ @队长「要改什么」 → 派写手返工（独立计数，不占三轮）→ 直接 @人
```

产物落盘（项目 `AGENTS.md` 有规定则**项目优先**，以下是兜底值）：

```
./design-facts/<需求名>/facts.md            事实清单
./design-reqdoc/<需求名>/reqdoc.md          需求文档
./design-prototype/<需求名>/index.html      可点击原型
```

`<需求名>` 取 **issue 标题原文**（标题就是需求名）。目录名一旦定下就**写死在派单说明里**，下游照抄，不要让各环节自己推导。

---

## 内网使用时注意

- **取代码**：`git clone` 或稀疏检出本目录即可；仓库里的中文文件名在 git 输出里会显示成八进制转义（如 `\346\226\271...`），属正常显示，文件本身没问题
- **daemon 的 GC 会删产出**：任务隔离目录闲置超过 `MULTICA_GC_TTL`（默认 24h）会被**整个删除**。所有智能体的产物必须回传 issue 或 push 到 git，只写在工作目录里等于消失
- **空闲看门狗会误杀长任务**：`MULTICA_OPENCODE_IDLE_WATCHDOG` 默认 10 分钟且**只能缩短不能延长**。所以每个提示词里都写了"每完成一个动作就输出一行进度"——任务粒度也要切小
- **多个 agent 共用一个本地目录会互相等锁**：目录处于 `in_place`（Direct）模式时，同一目录同时只能跑一个 run，其余状态是 `waiting_local_directory`。要么收敛并发，要么给不同 agent 分目录。**这是"运行中断/卡住"的高嫌疑原因**
- **失败后不会自动唤醒负责人**：系统会把 `in_progress` 回滚成 `todo`，然后静默躺住。看到状态是 `todo` 但已有阶段 label，那是失败回退、不是新单——去 execution log 点重试
- **改 runtime profile 参数后要重新注册 daemon**，已启动的任务仍用旧参数；升级 Multica 时先升服务端、再逐个升 daemon
- **原型官底座 B** 需要 OpenDesign daemon 在跑；底座 A 取真实系统截图**必须人工登录**，原型官会 @人 后停下等待（这是设计内的停顿，不是卡死）

---

## 相关

- 同仓库 [`opendesign-codeagent3-tutorial`](../opendesign-codeagent3-tutorial/)：把 codeagent3.0 接进 OpenDesign 的两条方案（含排坑清单）
- 同仓库 [`prototype-studio`](../prototype-studio/)：截真实系统 + 热区跳转的原型站生成器，原型官底座 A
